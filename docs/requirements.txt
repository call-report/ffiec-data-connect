# ReadTheDocs build requirements — the retired narrative docs are gone;
# the only thing RTD still builds is docs/source/index.rst, a one-page
# redirect landing that preserves the ffiec-data-connect.readthedocs.io
# URL. See docs/source/conf.py (extensions = []). These two packages
# mirror pyproject.toml [project.optional-dependencies.docs].
sphinx>=5.0.0
sphinx-rtd-theme>=1.0.0
